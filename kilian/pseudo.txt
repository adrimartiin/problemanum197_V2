    tirar commit mañana 
    X1 a X
        Si existe y si no esta vacío el input del form
        Definir linea como valor recogido del input
        Definir funcion descifrar_mensaje(cadena)
        Definir vocales como cadena =  "aeiouAEIOU"
        Definir frase_descifrada como cadena = ""
        Definir secuencia como cadena = ""

        Por cada i=0 hasta que cadena sea menor que el numero de caracteres de la variable 
        Definir letra = array genrado por cadenas junto a la iteracion
            Si  letra esta en vocales y no es falso
                Definir frase_descifrada como secuencia al reves + letra
                Igualar secuencia a cadena vacía

            Si no
                Definir secuencia como secuencia + letra
        
        Devolver frase_descifrada + secuencia al reves

        Definir descifradoFinal como  descifrar_mensaje(linea)
        Escribir Frase descifrada:  descifradoFinal
    
    X2 a X1
    INICIO

        //INTERACCION CON EL USUARIO
        Pido al usuario la frase
        Leo la frase

        //DEFINO FRASE ORIGINAL
        frase_Original = "frase del usuario"

        //INICIALIZO LAS VARIABLES
        longitud_frase_Original = longitud(frase_Original);
        i=0;
        frase_Transformada

        //BUCLE
        for (recorrer la i desde 0 hasta la longitud de la frase){
            If (i % 2 == 0) {
                frase_Transformada[] = frase_Original[i]
            }
        }

        for (recorrer la frase desde el final hasta la posicion 0)

            if (si no es par){
                frase_Transformada[] = frase_Original[i]
            }
            

        //MOSTRAR POR PANTALLA
        
    

        